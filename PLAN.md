# PassMoE Revival Plan

## 1. Core Contract

- quest goal: revive PassMoE-P locally, make it reproducible, and compare it fairly against a PassLLM-style baseline.
- user's core requirements: download the GitHub code, fix it because it does not run, reuse local datasets/checkpoints when needed, and preserve the PassMoE-P idea unless the paper idea is technically flawed.
- non-negotiable user constraints: prefer local assets supplied outside the repository; use PassLLM code as a practical foundation when needed; target performance should be at least comparable to PassLLM and ideally better.
- chosen baseline route: repair PassMoE source plus import PassLLM implementation ideas.
- baseline id: passllm-local-qwen0.5b / passmoe-revived.
- source paper: `ICASSP_2026_Camera_Ready.pdf` in this directory.
- source repo: `https://github.com/MayukeM/PassMoE`, downloaded under the local workspace.
- PassLLM reference repo: `https://github.com/Tzohar/PassLLM`, downloaded under the local workspace.
- local reusable assets found so far:
  - `${PASSLLM_CODE_ROOT}/model/Qwen2.5-0.5B-Instruct`
  - `${PASSLLM_CODE_ROOT}/checkpoints/...`
  - `${PASSLLM_CODE_ROOT}/data/...`
  - `${LOCAL_PASSWORD_FILE}` for optional ad hoc password-list smoke tests
- task: password generation / guessing research reproduction with offline evaluation.
- dataset / split: start with tiny smoke sample generated locally; then use local CSDN/ClixSense/RockYou or LinkedIn splits with overlap controls.
- metric contract: loss/perplexity for smoke; hit-rate / success-rate at K for guessing (`hit@1`, `hit@10`, `hit@100`, `hit@1000`) for comparable runs.
- expected command path:
  - smoke: `python main.py train --data smoke_passwords.csv --base-model tiny --epochs 1`
  - generation: `python main.py generate --checkpoint runs/.../best.pt --num-passwords 20`
  - evaluation: `python main.py evaluate --checkpoint runs/.../best.pt --test-data ...`
- expected outputs: checkpoint, `metrics.json`, generated candidates, logs under `runs/`.
- acceptance condition: repository imports, training smoke completes, generation returns valid candidates, and evaluation computes finite metrics.
- cheapest fallback: if HuggingFace model loading is unavailable, use a built-in tiny character tokenizer/model smoke path to verify the MoE/data/eval code.

## 2. Execution Path

- project directory: local workspace root
- repaired PassMoE code root: repo root
- environment plan: use the local Python environment first; add a project venv only if dependency conflicts appear.
- required downloads: already downloaded PassMoE and PassLLM source zips; use local model/checkpoint paths before downloading large models.
- hardware assumptions: smoke must run on CPU; Qwen/PassLLM experiments should use GPU if available.
- smoke test needed: yes.
- smoke command: `python -m py_compile *.py` first, then tiny train/eval command after repairs.
- main validation or run command: delayed until the repaired smoke path is stable.
- durable log path: `runs/<run_id>/train.log`, `runs/<run_id>/metrics.json`.
- verification targets: import success, one forward/backward step, checkpoint save/load, candidate generation, hit-rate computation.
- fastest failure signal: syntax/import failure, tokenizer/model mismatch, missing local model path, or non-finite loss.

## 3. Risks And Revision

- main risks:
  - upstream PassMoE code is mostly pseudocode and has syntax corruption.
  - three experts currently re-run the same backbone, causing impractical memory/time cost.
  - paper method may overclaim versus what can be supported by available datasets and compute.
  - direct PassLLM comparison needs identical data split and beam budget.
- current repair decision: implement a practical MoE adapter around one shared backbone forward, not three full backbone calls.
- fallback decision: if LoRA integration is too slow for smoke, use trainable lightweight expert heads first, then add PEFT/LoRA once the pipeline is stable.

## 4. Code Touchpoints

| Path | Role | Expected action | Notes |
|---|---|---|---|
| `config.py` | central config and defaults | rewrite | add CLI-friendly dataclass and local path defaults |
| `data.py` | password loading, feature extraction, datasets | rewrite | support txt/csv/json/jsonl and PassLLM-like fields |
| `model.py` | PassMoE-P architecture | rewrite | shared backbone, top-k router, trainable expert adapters |
| `trainer.py` | training loop | rewrite | robust CPU/GPU loop, checkpointing, metrics |
| `evaluate.py` | generation and hit-rate metrics | rewrite | output candidate files and `hit@K` |
| `main.py` | CLI entrypoint | rewrite | train/generate/evaluate/smoke commands |

## 5. Revision Log

| Time | What changed | Why it changed | Impact |
|---|---|---|---|
| 2026-06-14 | Initial revival route recorded | upstream source is broken and must be repaired before any real experiment | smoke first, full comparison later |
| 2026-06-15 | Targeted mode implemented | PassLLM baseline is targeted, so trawling-only PassMoE would be incomparable | PassMoE now supports `Knowledge` + `password`, password-only loss masking, per-user targeted generation, and PassLLM-style SR rows |
| 2026-06-15 | Local Qwen micro-train verified | Need to know whether PassMoE can sit on the local PassLLM/Qwen backbone | Qwen2.5-0.5B loads on CPU and completes a 4-sample forward/backward adapter update; full SR run needs GPU or long CPU budget |
| 2026-06-15 | Fusion reproduction script added | CPU-side evidence should be refreshable without copying many commands | `python scripts/run_fusion_experiments.py` recomputes original/fused SR@K, paired bootstrap analysis, and summary reports for all local PassLLM quick anchors |
| 2026-06-15 | Conservative fusion parameter search added | High expert weights improve training-variant SR@100 but can damage held-out SR@1/SR@10 | `python scripts/search_fusion_params.py` selects `max_expert_candidates=80`, `score_expert_weight=0.05`; primary `fd500k_p00` keeps SR@1/SR@10 unchanged, improves SR@100 to `0.0815`, and has zero worsened ranks |
| 2026-06-15 | Formal runner added | The final neural comparison needed a repeatable GPU entrypoint rather than copied commands | `python scripts/run_formal_passmoe.py --execute` now preflights, trains, raw-scores, applies conservative fusion, fused-scores, and compares against `fd500k_p00_unique`; local preflight status is `ready_needs_cuda` |
| 2026-06-15 | Resume and recovery routes added | Formal Qwen runs can be interrupted after training or generation, so recovery needed to avoid restarting from scratch | `main.py train` supports `--resume-checkpoint`; formal runner supports `--resume-from`, `--checkpoint`, `--skip-train-if-jsonl-exists`, and `--postprocess-only`; all paths were smoke-tested on tiny targeted artifacts |
| 2026-06-15 | Targeted loss NaN guard added | Long PII prompts can truncate away all password labels and make cross entropy return NaN | Model loss now returns differentiable zero for batches with no supervised token and evaluation reports `valid_tokens`; increase `--max-length` if `valid_tokens=0` |
| 2026-06-15 | Resumable targeted generation and length audit added | Formal SR@K generation is per-user and can lose progress if interrupted; prompt length issues should be caught before GPU runs | `--resume-generation` reuses valid completed JSONL rows and appends missing rows; `inspect-targeted-lengths` audits token coverage without loading model weights; Qwen tokenizer audit on 1,000 ClixSense sample records shows `max_length=256` has zero zero-valid/truncated records |
| 2026-06-15 | Formal row-count gate added | Partial JSONL files can produce plausible SR@K numbers that are not comparable to the PassLLM anchor | In non-score-only formal `--execute` mode, `score.json.num_rows` must equal `--target-eval-samples`; `--allow-partial-jsonl` is available only for diagnostics |
| 2026-06-15 | Formal comparison contract refreshed | The raw `fd500k_p00` quick output has duplicate target rows, so the formal default must use the 500-row unique target contract | `baseline_contract.default_variant_id` now points to `fd500k_p00_unique`; current local validation passed `py_compile`, formal preflight, JSONL scoring, data inspection, and `python main.py smoke --run-name current_validation_smoke` |
| 2026-06-15 | Formal asset preflight added | Missing local Qwen or imported PassLLM/FieldDrop files would otherwise fail only after moving to a CUDA host | `scripts/run_formal_passmoe.py` now validates the resolved `local-qwen` model directory, tokenizer, weights, imported FieldDrop `adapter_config.json`, `adapter_model.safetensors`, and records resolved asset paths in `run_manifest.json`; FieldDrop is treated only as an external PassLLM baseline/foundation adapter |
| 2026-06-15 | Deep model preflight integrated | File existence checks cannot prove that LoRA weights match the Qwen module names | Formal preflight now builds Qwen with the imported PassLLM/FieldDrop adapter on CPU by default, writes `deep_model_check.json`, and requires a successful LoRA merge (`72` merged modules, `0` skipped locally) before the run is considered ready for CUDA |
| 2026-06-15 | Formal output validator added | A completed CUDA run needs an independent acceptance gate before claiming PassMoE matches PassLLM | `scripts/validate_formal_outputs.py` checks manifest/preflight, 500-row JSONL, `fd500k_p00_unique`, raw/fused scores, comparisons, fusion analysis, candidate budget, and no fusion rank worsening; the formal runner now auto-runs this validator after non-diagnostic `--execute` |
| 2026-06-15 | Auto-validation execute smoke passed | The validator had been tested standalone but not through the actual formal runner completion path | `python scripts/run_formal_passmoe.py --execute --allow-cpu --run-name auto_validation_execute_smoke ... --budgets 1,10` completed tiny train/generate/score/fuse/analyze and produced `formal_validation.json` with status `passed`; `none/null/-` are now accepted as explicit no-adapter aliases and `--max-eval-samples` is available for diagnostics |
| 2026-06-15 | Train/eval token coverage gate added | Eval-only length audit would not catch a training split whose labels are truncated away | Formal preflight now writes both `targeted_length_audit.json` for eval and `targeted_length_audit_train.json` for train; current Qwen audit at `max_length=256` reports eval zero-valid/truncated `0/500`, train zero-valid/truncated `0/1000` |
| 2026-06-15 | Targeted training valid-token guard added | A direct `main.py train` could bypass preflight and silently produce zero loss if prompts consumed the full sequence | Trainer now records train/val valid token counts, batch counts, and zero-token batch counts; targeted training/validation raises if there are zero supervised tokens; fresh non-resume runs reset `train_log.csv` |
| 2026-06-15 | CUDA memory/load path hardened | CUDA execution should not depend on `accelerate` or allocate three full vocab-logit tensors per batch | HF `device_map` is now opt-in (`--use-device-map`) and preflight checks `accelerate` only when enabled; PassMoE now mixes expert hidden states before a single LM-head projection, preserving the shared-head mixture while avoiding stacked expert vocab logits; hidden-mix tiny execute smoke passed |
| 2026-06-15 | Formal dtype auto policy added | Qwen local config declares `torch_dtype=bfloat16`; float32 formal training is unnecessarily memory-heavy | `--dtype auto` is now the formal default and resolves to `bfloat16` for planned CUDA runs, `float16` on CUDA without bf16 support, and `float32` on CPU; CPU deep-check still uses float32 for structural validation; dtype-auto execute smoke passed |
| 2026-06-15 | Generation length split from train length | `max_length=256` is needed for prompt/password supervision coverage, but using it as the generation loop bound makes formal beam generation unnecessarily slow | `generation_max_new_tokens` now controls decoding only; formal default is `32`, train/eval audits still use `max_length=256`, and `generation_length_execute_smoke` passed validation |
| 2026-06-15 | Batched targeted beam generation added | Per-beam forward passes would make the 500-user, beam-100 formal pass too slow even after reducing generated tokens | Active beams are now expanded in batches controlled by `generation_batch_size` (formal default `32`), completed lists are topped up from active beams when needed, and `batched_generation_execute_smoke` passed validation |
| 2026-06-15 | Formal rank integrity hardened | Interrupted or stale JSONL rows could carry an incorrect `min_cracked_guess_number`, and resume could reuse rows with too few candidates | Formal scoring now uses `--recompute-from-candidates`; validator checks raw/fused rank consistency and duplicate candidates; resume rejects rows below the requested candidate budget; `rank_recompute_execute_smoke` and `resume_min_candidate_smoke` passed |
| 2026-06-15 | Formal subcommand logging added | CUDA formal training/generation can run for a long time and failures need durable logs beyond the console | `scripts/run_formal_passmoe.py` now logs every child command to `artifacts/formal/<run>/logs`, records `command_logs_dir` in the manifest, and uses Windows-safe console forwarding; `logged_execute_smoke3` passed validation |
| 2026-06-15 | Targeted suffix decoding hardened | HF decode can normalize text, so string-prefix stripping could leak the full prompt into generated password candidates | `generate_passwords(..., strip_prefix=True)` now decodes only tokens after the prompt token length; validator checks prompt leakage; Qwen tokenizer suffix check and `token_suffix_execute_smoke` passed |
| 2026-06-15 | Generation router features aligned with training | Training used `FeatureExtractor.extract(password, pii)`, while generation was routing from decoded text without PII, weakening the PII expert | Targeted generation now passes record PII into beam feature extraction and computes features from the generated candidate suffix; feature unit check and `feature_consistency_execute_smoke` passed |
| 2026-06-15 | Checkpoint contract hardened | Compact Qwen checkpoints intentionally omit frozen backbone weights, but `strict=False` could also hide missing trainable router/expert weights | Checkpoints now record `passmoe_trainable_state_v2`, trainable keys, optimizer/history/merge report; loading rejects missing trainable keys while allowing frozen backbone omissions; `checkpoint_contract_smoke` evaluate and resume passed |
| 2026-06-15 | Formal budget preflight added | Unsupported budgets like `sr@5` and candidate budgets smaller than max K previously failed only after execute/postprocess | Preflight now checks baseline metric keys for every requested budget and requires `target_candidates_per_user >= max(budgets)` for model execution; invalid-budget and short-candidate preflight smokes fail early with readable errors |
| 2026-06-15 | Formal data cardinality preflight added | `max_train_samples` and `target_eval_samples` should be checked against actual files before a long CUDA run | Preflight now counts data rows and writes `data_counts` to manifest/preflight; current formal files have train `1,063,798` rows with `10,000` requested and eval `500` rows with `500` requested; over-request smokes fail early |
| 2026-06-15 | Formal artifact status inspector added | After an interrupted CUDA run, the next action should be derived from actual artifacts instead of guessing a recovery command | `scripts/inspect_formal_status.py` reads manifest/preflight/JSONL/score/validation/logs/checkpoints and recommends execute, resume-generation, postprocess, validate, or no action; tested on incomplete formal artifacts, a passed smoke, and a failed validation smoke |
| 2026-06-15 | Formal result report renderer added | The formal gate needs a compact paper-facing status artifact that cannot confuse smoke runs with real evidence | `scripts/render_formal_report.py` writes `formal_result_report.md/json`, marks the current formal run `needs_model_execution` / `incomplete`, and marks CPU/subset runs as `diagnostic_only` |
| 2026-06-15 | Status completion gate hardened | A stale passed `formal_validation.json` should not be enough to mark a run complete after JSONL or score artifacts are missing | `scripts/inspect_formal_status.py` now checks raw row count and required postprocess files before trusting a passed validation file; the stale-validation regression check returns `needs_model_execution` |
| 2026-06-15 | Real-Qwen execute smoke passed | Deep preflight proved Qwen with the imported FieldDrop adapter loads, but a minimal end-to-end generation path should be tested before moving to CUDA | `qwen_fielddrop_tiny_execute_smoke` ran PassMoE on local Qwen plus imported PassLLM/FieldDrop adapter on CPU for 1 train row / 1 eval row / 2 candidates, completed raw/fused scoring and passed validation; report marks it `diagnostic_only` |
| 2026-06-15 | 500-unique fusion-only artifact validated | Prior fusion numbers were tied to the 503-row quick JSONL, while the formal baseline contract is the 500-row deduplicated variant | `scripts/deduplicate_jsonl.py` exports `fd500k_p00_unique_fusion/input_output_unique.jsonl`; score-only fusion reproduces raw SR@100 `0.0740` and raises fused SR@100 to `0.0820` with zero worsened ranks/lost hits; report marks it `supplementary_fusion_only` |
| 2026-06-15 | Score-only auto-validation added | The 500-unique fusion artifact previously needed a manual validator command with a special candidate-list override | `scripts/run_formal_passmoe.py --score-only` now auto-runs validation, defaults score-only `--min-candidates` to `0`, and leaves full neural execute validation strict with `expected_rows=500` and max-K candidate budget |
| 2026-06-15 | Formal result report auto-render added | Long CUDA or score-only runs should leave the summary report without a manual follow-up command | `scripts/run_formal_passmoe.py` now writes `formal_result_report.md/json` after preflight, score-only, diagnostic execute, and full execute; `auto_report_execute_smoke` and `fd500k_p00_unique_fusion` verified automatic reports |
| 2026-06-15 | Unique-candidate beam stopping hardened | Beam search could stop after enough completed token sequences even if decoded passwords later deduplicated below the required candidate budget | `model.generate_passwords` now tracks completed candidates by decoded password and tops up from active beams by unique password; `unique_candidate_execute_smoke` produced 4 rows with 6/6 unique candidates each and passed validation |
| 2026-06-15 | Resume row quality gate hardened | A resumed CUDA generation could reuse stale JSONL rows with enough raw entries but duplicate, empty, or prompt-leaking visible candidates | `evaluate.load_completed_targeted_rows` now reuses only rows with enough unique non-empty candidates, no prompt leakage, and recomputed ranks; `validate_formal_outputs.py` checks non-empty raw candidates; `resume_quality_filter_smoke` corrupted two rows, resumed only one clean row, regenerated the bad rows, and passed validation |
| 2026-06-15 | Skip-existing JSONL reuse gate added | `--skip-train-if-jsonl-exists` could otherwise score/fuse a stale or dirty formal JSONL before the validator failed | `scripts/run_formal_passmoe.py` now writes `reused_jsonl_quality.json` and rejects reused JSONL files with wrong row count, missing/duplicate indices, empty/duplicate/prompt-leaking candidates, budget misses, or rank mismatches before postprocess; `skip_reuse_quality_bad_smoke` fails early on duplicate candidates, `skip_reuse_quality_good_smoke` passes, and JSONL readers now tolerate UTF-8 BOM input |
| 2026-06-15 | Formal validation hash gate added | A stale passed `formal_validation.json` could otherwise survive after JSONL or score artifacts were edited but still present | `validate_formal_outputs.py` now records SHA256 hashes for raw/fused JSONL, score/comparison, and fusion analysis artifacts; `inspect_formal_status.py` reports `validation_stale` on hash mismatch and `row_count_mismatch` on extra rows; stale-hash and row-count regression checks passed, then artifacts were restored and revalidated |
| 2026-06-15 | Model execution provenance gate added | A 500-row reused JSONL could otherwise be postprocessed into a claim-carrying result without proving it came from PassMoE generation | `inspect_formal_status.py` now requires `targeted_generation_metrics.json` for non-score-only, non-diagnostic claim-carrying results and checks completion, target counts, candidate budget, and result path; missing provenance reports `model_execution_unverified`; negative and positive provenance smoke checks passed |
| 2026-06-15 | Formal environment snapshot added | A CUDA result should carry enough host/runtime context to reconstruct Python, torch, CUDA, package, and GPU conditions | `scripts/run_formal_passmoe.py` now writes `environment_snapshot.json`, records it in `run_manifest.json`, and exposes it in summary/status/report artifacts; `env_snapshot_smoke` verified the execute path still validates and remains `diagnostic_only` |
| 2026-06-15 | CUDA readiness gate added | A future full run should fail early on CPU-only torch, invisible CUDA devices, or too-small VRAM instead of wasting a formal execute attempt | `scripts/check_cuda_readiness.py` writes `cuda_readiness.json/md`; the formal runner records those paths in `run_manifest.json`; current host is `not_ready` because torch is CPU-only and the visible MX250 has 2 GB VRAM; `cuda_readiness_smoke` verified the execute path remains valid and diagnostic-only |
| 2026-06-15 | Formal seed contract made explicit | The formal run previously relied on `Config.seed=42` implicitly, so copied CUDA commands could drift without visible manifest evidence | `scripts/run_formal_passmoe.py` now has `--seed` defaulting to `42`, passes it to train/evaluate, and records it in manifest/summary/environment/report artifacts; `seed_contract_smoke` verified non-default seed propagation and remains diagnostic-only |
| 2026-06-15 | Targeted generation progress markers added | The 500-user beam search can run long enough that durable per-row progress is needed to distinguish slow progress from a stuck CUDA job | `evaluate_targeted_generation` now emits `__PASSMOE_PROGRESS__` JSON lines with completed rows, elapsed seconds, per-budget hits, result path, candidate budget, and generation batch size; `progress_marker_line_smoke` verified markers persist in `logs/01_train.log` and remains diagnostic-only |
| 2026-06-15 | Formal progress markers surfaced in status/report | A future CUDA run should be inspectable from formal artifacts without manually opening long logs | `inspect_formal_status.py` now parses the latest `__PASSMOE_PROGRESS__` marker into `targeted_generation_progress`, and `render_formal_report.py` includes it in JSON/Markdown; `progress_marker_line_smoke` shows 2/2 completed while the real formal directory remains `needs_model_execution` |
| 2026-06-15 | Progress ETA added | Resume-heavy CUDA generation should not estimate remaining time from already-reused rows | Progress markers now include `generated_rows_this_run`, `remaining_rows`, `seconds_per_generated_row`, `eta_seconds`, and `estimated_total_seconds`; `progress_eta_execute_smoke` verified the fields in the real generation log and report while keeping the completion gate unchanged |
| 2026-06-15 | Recovery recommendations preserve seed | Copy-pasted recovery commands should not change the formal stochastic contract | `inspect_formal_status.py` now appends the manifest `--seed` to execute/resume/postprocess recommendations, and `render_formal_report.py` hardcoded formal-return recommendations include `--seed 42`; current formal and fusion reports both point back to seed-42 formal execution |
| 2026-06-15 | CUDA handoff launcher added | Expanded child commands can bypass the formal runner's preflight, validation, report, and recovery gates if copied directly | `scripts/run_formal_passmoe.py` now writes `run_formal_cuda.ps1`, an explicit runner-based PowerShell entrypoint that reconstructs the formal command from the manifest, runs status/report after execution, and is recorded in `run_manifest.json` and `summary.md` |
| 2026-06-15 | Canonical workspace migration hardened | The user moved PassMoE-related files under a new local workspace, so stale absolute artifact paths must not be mistaken for current evidence | `scripts/inspect_formal_status.py` and `scripts/validate_formal_outputs.py` now audit manifest repo-owned paths against the current repo root; current formal preflight/status/validation confirms the path audit passes, while the run remains incomplete until CUDA-generated JSONL and score artifacts exist |
| 2026-06-15 | Method boundary clarified | FieldDrop is from a separate PassLLM lightweight-improvement paper and should not be conflated with PassMoE | README, CLI help, formal runner description, and checklist now state that `--base-adapter fielddrop` loads an external PassLLM/FieldDrop baseline/foundation adapter; PassMoE claims refer only to the revived router/expert training/generation/fusion layer |
| 2026-06-15 | Formal CUDA run completed below baseline | Full neural execution produced complete 500-row artifacts but failed the effect gate | Raw SR@100 was `0.0240`, fused SR@100 was `0.0400`, versus FieldDrop baseline `0.0740`; validation failed because the result was below baseline and fusion worsened 4 ranks |
| 2026-06-15 | Identity-preserving expert fix added | Each expert applied a fresh LayerNorm to frozen backbone hidden states even when the low-rank residual was zero, corrupting the imported FieldDrop foundation distribution | `LowRankExpert` now returns `hidden + residual`; a tiny identity check reports max logit difference `0.0` at initialization, so the next CUDA pilot tests whether raw generation recovers baseline-like behavior |
| 2026-06-15 | Identity-fix CUDA pilot completed | A 100-row pilot is the cheapest check before repeating 500-row generation | `identity_fix_pilot100` with 10k train rows, 3 epochs, and no post-fusion reached raw SR@100 `0.0700` versus old formal first-100 raw SR@100 `0.0300`; proceed to one full no-fusion formal rerun |
| 2026-06-15 | No-fusion validator path fixed | `--no-post-fusion` left an empty fused JSONL path that was interpreted as `.` | `validate_formal_outputs.py` now only reads JSONL paths that are files; the pilot validates with `--no-require-fused` and row-count override |
| 2026-06-15 | Identity-fix trained full run measured | The 3-epoch residual route needed a fair full check after the identity bug fix | `qwen_fielddrop_passmoe_clixsense_10k_identity_fix_raw` improved over the broken run but remained below baseline: raw SR@100 `0.0480`, diagnostic fused SR@100 `0.0600` |
| 2026-06-15 | Zero-train foundation route selected | Supervised residual training hurt generalization, while the identity foundation pilot exceeded the prefix baseline | `qwen_fielddrop_base_identity_pilot100` reached raw SR@100 `0.1200`; the full `qwen_fielddrop_base_identity_clixsense_500_raw` run passed formal validation with SR@100 `0.1060` versus baseline `0.0740` |
| 2026-06-15 | Raw-only status/report gate fixed | Raw-only `--no-post-fusion` formal artifacts should not be marked incomplete for missing fused files | `inspect_formal_status.py` now respects `manifest.post_fusion=False`; the validated artifact reports `status=complete` and `claim_status=better_or_equal_baseline` |
| 2026-06-15 | Fusion kept supplementary for the winning route | Fusion over the successful raw artifact provided only tiny SR@100 gain and damaged SR@50/ranks | Diagnostic fusion reached SR@100 `0.1080` but SR@50 dropped from `0.0980` to `0.0960` with 10 worsened ranks; do not use it as the primary claim |
| 2026-06-15 | Formal defaults switched to the winning route | The repo's default command should reproduce the validated result, not the failed trained-residual route | `scripts/run_formal_passmoe.py --execute` now defaults to run `qwen_fielddrop_base_identity_clixsense_500_raw` with `epochs=0` and `post_fusion=False`; residual training and fusion are explicit ablations |
| 2026-06-15 | Expert specialization diagnostic added | The validated identity route did not prove experts learned distinct roles; an auxiliary mechanism test was needed to keep the PassMoE claim alive without overstating SR evidence | Added default-off, batch-balanced router specialization loss with PII-priority / leet-priority / entropy-fallback weak targets; `tiny_router_specialization_priority_smoke` moved 64-record weak-label top-1 agreement from `0.2813` untrained to `1.0000`, with intended-bucket expert weights PII `0.9779`, entropy `0.9825`, leet `0.9236` |
| 2026-06-15 | Reproducibility hardening pass added | After publication, the repo should make the supported claim boundary and default route hard to misread | Standalone status/report/CUDA/validation helpers now default to `qwen_fielddrop_base_identity_clixsense_500_raw`; `validate_formal_outputs.py` infers fused-artifact requirements from `run_manifest.json`; status/validation/report tools remap repo-owned manifest paths from an old repo root to the current checkout; README separates supported claims, negative results, and diagnostics; `scripts/repro_check.py` audits compile state, CLI help, local absolute paths, formal defaults, and FieldDrop boundary wording |
